# =====================
# Database (PostgreSQL)
# =====================
# make dev-native（ネイティブ起動）の場合は localhost を使用
# make dev（Docker Compose）の場合は docker-compose.yml 側で db に上書き済み
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/decision_loop"

# =====================
# API Server
# =====================
# PORT=3001

# =====================
# AI Service
# =====================
# PORT=8001

# Azure Service Bus（ローカル: Service Bus Emulator を使用）
# make dev-native（ネイティブ起動）の場合は localhost を使用
# make dev（Docker Compose）の場合は docker-compose.yml 側で servicebus に上書き済み
AZURE_SERVICE_BUS_CONNECTION_STRING="Endpoint=sb://localhost;SharedAccessKeyName=RootManageSharedAccessKey;SharedAccessKey=SAS_KEY_VALUE;UseDevelopmentEmulator=true"
AZURE_SERVICE_BUS_QUEUE_NAME="decision-loop"

# =====================
# AI Service（Azure OpenAI）
# =====================
# AZURE_OPENAI_ENDPOINT="https://xxxx.openai.azure.com/"
# AZURE_OPENAI_API_KEY="..."
# AZURE_OPENAI_DEPLOYMENT_NAME="gpt-4o"
# AZURE_OPENAI_API_VERSION="2024-02-15-preview"

# AI Service → API Server 内部通信（ネイティブ起動時）
# make dev（Docker Compose）では docker-compose.yml 側で http://api:3001 に上書き済み
# INTERNAL_API_BASE_URL="http://localhost:3001"

# =====================
# OIDC（API 側の認証ミドルウェアが ID トークン検証に使用）
# =====================
# 未設定時は services/fake-auth の既定値（http://localhost:3007 / fake-auth-client）にフォールバックする。
# 本番（Entra ID 等）に切り替える際は、テナント固有の issuer / API の audience(=Application ID URI など) で上書きする。
# 例:
# OIDC_ISSUER_URL="https://login.microsoftonline.com/<tenant-id>/v2.0"
# OIDC_AUDIENCE="api://<application-id>"
# OIDC_ISSUER_URL="http://localhost:3007"
# OIDC_AUDIENCE="fake-auth-client"
